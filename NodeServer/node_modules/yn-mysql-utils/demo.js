const mysql = require('./index')

let data = new mysql({
    host     : '127.0.0.1',
    user     : 'root',
    password : 'lb714500',
    database : 'SmallActivity',
    connectionLimit : 10
})

data.Query({
    sql: 'select * from activity_page where id = ?',
    par: [2]
}).then(res => {
    console.log(res)
})